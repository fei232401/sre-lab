# 决策日志(ADR)

> **这是什么**:ADR = Architecture Decision Record,架构决策记录。工程界真实存在的实践——**每个重要技术决策都留下一份"当时有哪些选项、为什么选这个"的记录**。
>
> **为什么要有它**:
> 1. 三个月后你会忘记为什么这么选,只剩"当时好像是这么定的"
> 2. 面试官问"你为什么选 X 不选 Y",翻这篇就有答案
> 3. 写 ADR 这个习惯本身就是加分项——大部分应届生没有
>
> **格式**:每条决策记录 背景 / 考虑过的选项 / 决定 / 理由 / 后果与代价 / 何时应该重新评估

---

## D1:CI 引擎选 Jenkins

- **日期**:2026-09-15(v1 曾选 GitHub Actions,v2 推翻)
- **状态**:✅ 已决定

### 背景

需要为「游戏运维工程师」岗位搭建可实操的 CI 流水线。本机无任何 CI 组件。

### 考虑过的选项

| 选项 | 优点 | 缺点 |
|---|---|---|
| **GitHub Actions(托管)** | 零搭建,与仓库天然集成 | 企业内网/私有部署场景覆盖不到;面试官可能觉得"没自己搭过" |
| **GitHub Actions + self-hosted runner** | 兼顾:写 workflow(CI 能力)+ 维护 runner(运维能力) | 国内游戏行业实际很少用 |
| **Jenkins on K8s** | 国内游戏/传统企业事实标准,K8s Plugin 动态 agent 是生产标准姿势;官方 Helm Chart 一条命令即起 | 引入成本(约 +0.5 天);内存占用 |
| **GitLab CE 自建** | 全家桶 | 单体吃 4-6G 内存,与监控栈抢资源 |
| **Tekton** | K8s 原生,CNCF,与 ArgoCD 同生态 | 学习曲线陡,面试官未必熟 |
| **Drone / Woodpecker** | 轻量 | 社区活跃度下降,不投入 |

### 决定

**Jenkins on K8s(官方 Helm Chart)+ Kubernetes Plugin 动态 agent。**

### 理由

1. **岗位对口**:国内游戏行业 Jenkins 是事实标准,用户本人判断"真实环境绕不开",这是准确的
2. **一次拿下两个面试维度**:写 Jenkinsfile 是 CI 能力;维护 Jenkins 本体(controller 单点、agent 资源隔离、构建缓存)是运维能力
3. **动态 agent 是核心价值**:每次构建起一个 Pod,用完销毁——构建环境隔离、不用常驻一堆 agent 机器

### ⚠️ 被推翻的错误判断(v1 → v2)

**v1 否决 Jenkins 的理由是"Jenkins + ArgoCD = 两套 CD 混乱"——这个理由是错的。**

正确认知:**Jenkins 做 CI(构建/测试),ArgoCD 做 CD(部署),这是标准分工,不是重叠。** 只要 Jenkins 不碰部署,就没有任何冲突。

**教训**:否决一个方案的"理由"本身需要被审视。理由站不住,结论就要重来。

### 后果与代价

- 工期 +0.5 天(M3 从 1 天 → 1.5 天)
- 本机内存压力增加(约 1-1.5 GB),需要停掉闲置的 `k3d-ew-cluster`
- 新增一个待掌握的知识面:Jenkinsfile 语法、Kubernetes Plugin、agent Pod 模板

### 何时重新评估

如果本机内存实在吃紧,退路是:Jenkins 跑在宿主机 docker(而非集群内),省掉 controller 的集群内开销。

---

## D2:CD 引擎与同步策略

- **日期**:2026-09-15 提出,2026-09-16 细化
- **状态**:✅ 已决定(含一处修正)

### 背景

用户对 ArgoCD 有负面体验:

> "之前开了 synced,很多改了的配置没有 git commit 都被应用失败,导致让我对他没什么好感。一旦用它管理 apps,所有修改都要走 git,个人开发环境我觉得比较麻烦,要么是不开 auto synced,要么换个方案。"

备选疑问:"Jenkins 本身不就能 CD 吗?"

### 诊断

用户遇到的现象**不是 ArgoCD 故障,是 `selfHeal: true` 在正确工作**——手动改的配置被自动纠正回 Git 版本。用户当年大概率把三个开关(`auto-sync` / `selfHeal` / `prune`)全开了。

详见 `05_学习笔记/01_ArgoCD到底是什么.md`。

### 考虑过的选项

| 选项 | 优点 | 缺点 |
|---|---|---|
| 保留 ArgoCD,关掉 auto-sync | 不烦了 | 失去 GitOps 的便利性(每次都要手动点同步) |
| 换回 Jenkins push CD | 简单,已会 | 丢失漂移检测;凭证风险;少一个高价值面试素材 |
| **保留 ArgoCD + 按应用性质分级配置同步策略** | ✅ 兼顾 | 需要理解三个开关的作用 |
| 两个都做,做对照实验 | 素材最全 | +0.5 天 |

### 决定

1. **保留 ArgoCD 作为 CD 主线**
2. **同步策略按应用性质分级**:
   - 基础设施类(monitoring / jenkins / argocd 自身):`selfHeal: true` — 防漂移 = 保命
   - 开发中的应用(game-server):`selfHeal: false` + `prune: false` — 允许手动调试
3. **先做一组对照实验再固定配置**(全关 → 开 selfHeal → 开 prune,观察行为差异)
4. **额外做一条 Jenkins push CD 作为对照组**,同一次发布两条路都跑

### 理由

1. **用户的痛点是真实的,但不是 ArgoCD 的必然**——是配置问题,有第三条路
2. **"开发环境半开 / 生产环境全开"的配置差异本身是高价值面试素材**——大多数候选人答不上"GitOps 在个人环境不麻烦吗"这个问题
3. **push vs pull 对照实验直接回答"Jenkins 能不能 CD"**,这是面试必问

### 后果与代价

- M1 需要增加对照实验环节
- M3 Jenkinsfile 需要加一个 push 部署的 stage(约 +0.5 天)

### 何时重新评估

无。这是本设计里最"生产对齐"的一块。

---

## D3:镜像构建方式(Buildah rootless)

- **日期**:2026-09-16
- **状态**:✅ 已决定

### 背景

Jenkins agent 是动态 Pod,**在 Pod 里怎么构建容器镜像**?这是 Jenkins on K8s 最经典的坑。

### 考虑过的选项

| 选项 | 安全性 | 复杂度 | 说明 |
|---|---|---|---|
| 挂载宿主机 `docker.sock` | ❌ **极差** | 最低 | 等于把宿主机 root 权限交给构建容器 |
| DinD(docker-in-docker) | ❌ 差 | 中 | 需要 `privileged` |
| **Buildah(rootless)** | ✅ 好 | 中 | CNCF Sandbox(2025-01 加入),daemonless + rootless 优先 |
| BuildKit / buildx | ✅ 好 | 中 | 构建性能最好,缓存强;rootless 部分环境受限 |
| ~~Kaniko~~ | — | — | ⚠️ **2025 年 6 月已被 Google 归档**,不再维护,新项目不应选 |

### 决定

**Buildah rootless。**

### 理由

1. **不挂 docker.sock**——生产大忌,写进 runbook 当反面教材
2. Buildah 被 GitLab 等主流平台推荐为 Kaniko 的替代(2025 年社区共识)
3. 契合 K8s Job 模型:起 Pod → 构建 → 推送 → 退出

### 后续影响

- 需要研究 Buildah 在 Pod 内的运行配置(`--storage-driver vfs --isolation chroot`)
- "为什么不能挂 docker.sock"作为面试素材写进 runbook

---

## D4:改造存量集群 vs 清场重建

- **日期**:2026-09-15
- **状态**:✅ 已决定

### 背景

本机有一个运行 43 天的 `k3d-ai-cluster`,其中 `ai-platform` 命名空间有 6 个异常 Pod(vllm-3b 全烂),各组件重启 6~24 次。

### 考虑过的选项

| 选项 | 优点 | 缺点 |
|---|---|---|
| A. 清场重建 | 干净、可重复、一条命令起环境 | 丢掉真实的脏数据;**没有真故障素材** |
| B. 改造存量 | 有真实故障可排障;复用已有监控栈省半天 | 集群脏,需要纪律 |
| C. 双集群全做 | 两个故事都有 | 工作量翻倍 |

### 决定

**B — 改造存量**,把 A 的脚本化作为 M5 收尾产出。

### 理由

**面试官想听的"运维"不是"我搭了个新东西",而是"线上出事了我怎么定位和恢复"。**

本机躺着一个 43 天没人管、Pod 重启 24 次、vllm 全烂的集群——这是白送的、**真实的**排障素材。

### 佐证:盘点已挖出三个真问题

1. `helm list -A` 返回空 → 根因是 **kubeconfig context 错配**(Helm 一直在查另一个集群)
2. **M1 其实是两件事**:monitoring/kube-system 由 Helm 管理,ai-platform 由手工 `kubectl apply` 管理——**两套接管策略**
3. PushPlus token 明文硬编码在公开仓库

→ **"M0 可能没料"的风险已显著降低。**

### 翻车预案

如果 M0 诊断下来发现故障确实平淡(如仅因 WSL 无 GPU 导致 vllm 起不来),**如实告知用户并立即转 A**,不硬凑素材。

---

## D5:现成方案优先(Adopt / Reference / Reject)

- **日期**:2026-09-16(用户提出:"看看GitHub上有没有什么现成可用的内容,省下我们时间,可以专注于过程和原理以及问题处理,而减少实操")
- **状态**:✅ 已决定

### 决定

**能用现成的就用现成的,把时间花在"为什么这么设计"和"出问题怎么查"上,而不是手搓组件。**

所有评估过的项目分三类记录,详见 `00_设计定稿.md` 第八节:

- ✅ **Adopt**:`jenkinsci/helm-charts`、`samber/awesome-prometheus-alerts`、`chaos-mesh/chaos-mesh`、`trivy`、`grafana/loki` chart
- 📖 **Reference**(读它、理解它,不装它):`name5566/leaf`(Go 游戏服框架长什么样)、`argocd-image-updater`(另一种镜像更新路径,但"CI 回写 GitOps repo"必须自己写一遍才真懂)
- ❌ **Reject**:`actions-runner-controller`(已转 Jenkins)、ChaosBlade(非 K8s 原生)、DinD/docker.sock(安全)

### 理由

**选型本身就是面试考点。** 面试官问的不只是"你会不会用",更是"你为什么选它、评估过什么替代品、它的坑在哪"。留下 Reject 的理由,比留下 Adopt 的清单更能证明判断力。

### 边界

**"用现成方案"不适用于学习目标本身。** `argocd-image-updater` 能替我们做镜像更新,但"CI 回写 GitOps repo"是 GitOps 最核心的概念——交给工具等于跳过学习目标。**判断标准:这个东西本身是不是我们要学的内容。**

---

## D6:M1 处理存量 GitOps 结构的方式

- **日期**:2026-09-16
- **状态**:✅ 已决定
- **触发**:M0 发现 5

### 背景

M0 盘点发现 `sre-lab-gitops/production/` 下有 **31 个文件**,最后提交于 **2026-06-29**,集群里**一个对象都没部署**。包括完整的 App-of-Apps bootstrap、Loki/Promtail/sealed-secrets 清单,以及两个专门解决 k3s 告警假阳性的补丁。

同时存在一个结构性问题:**同一份内容有四份副本**。

```
sre-lab-gitops/manifests/monitoring-stack/wechat-adapter.yaml
sre-lab-gitops/production/monitoring/wechat-adapter.yaml
sre-lab-gitops/k3s-ai-platform/environments/prod/monitoring/wechat-adapter.yaml
sre-lab-gitops/k3s-ai-platform/reference/baseline-manifests/monitoring-stack/wechat-adapter.yaml
```

### 考虑过的选项

| 选项 | 优点 | 缺点 |
|---|---|---|
| **A. 审计后修复复用** | 省时间;能对照"当时思路错在哪",本身是排障素材 | 需要先花时间审计 |
| B. 清掉重写 | 干净、口径统一 | +0.5 天;丢掉 6/29 的思路 |
| C. 先只接管 monitoring | 风险最低 | 丢失 GitOps"纳管全部"的价值;面试故事弱 |

### 决定

**A — 审计后修复复用。**

### 理由

1. **"已有的不等于正确的"**——6/29 那一版显然卡在某一步没走完。**搞清楚它为什么没走完,比重新写一遍有价值**:这正是运维岗最核心的能力(接手别人的烂摊子并理清)
2. **那套结构里有真东西**:App-of-Apps 的 bootstrap 骨架、k3s 假阳性补丁、Sealed Secrets 模板——重写等于把已有的思考扔掉
3. **面试素材**:"我接手了一个半年前同事留下的 GitOps 目录,发现它有四份重复副本,我先做的不是部署而是理清哪份是真相来源"——这比"我从零搭了套 ArgoCD"更接近真实工作

### 必须先解决的两件事

1. **四份重复目录 → 定出唯一真相来源**
2. **`root-app.yaml` 开着 `prune: true` + `selfHeal: true`,且指向 `bootstrap/` 目录**——一旦部署会创建 6 个子 Application,**部署前必须 dry-run**

### 对 `k3s-alert-patches.yaml` 的评价

原文用一条同名 `PrometheusRule` 覆盖默认规则,表达式写成:

```promql
absent(up{job="kube-proxy", namespace="k3s-does-not-have-kube-proxy"} == 1)
```

**靠一个不存在的 namespace 让规则永远为 false。** 注释里自称"降维打击"。

**这能工作,但手法值得商榷。** 问题不在于能否工作,在于**读者需要理解"为什么这个 namespace 不存在"才能看懂它在干什么**——这是典型的"聪明但不可维护"。

**更干净的路径**:在 Helm values 里直接关掉对应的 rule group。kube-prometheus-stack 的 values 支持逐条启用/禁用默认规则。

**留待 M1 拍板。**

### 后果与代价

- M1 工期 +0.5 天(审计)
- 需要用户在若干拍板点确认

---

## D7:内存硬约束的解法

- **日期**:2026-09-16
- **状态**:✅ 已决定
- **触发**:M0 发现 6

### 背景

```
agent-0   6.499GiB / 8GiB   81%  ← 唯一有 GPU,被监控栈(Prometheus)占满
agent-1   871MiB  / 8GiB   11%  ← 几乎空着
ew-cluster 746MiB               ← 闲置(项目二,已暂缓)
```

**加 Jenkins(~1-1.5G)+ ArgoCD(~0.5G)会顶爆 agent-0。**

### 考虑过的选项

| 选项 | 收益 | 代价 |
|---|---|---|
| **A. 停 ew-cluster + nodeSelector 钉 agent-1** | 避开瓶颈,零重建 | 需要验证 agent-0 负载是否需重新平衡 |
| B. 停 ew-cluster + 提高 agent-0 上限到 12G | 直接扩容 | **需重建集群容器 → 丢 43 天真实状态** |
| C. 两个都做 | 最保险 | 同样触发重建 |

### 决定

**A。**

### 理由

1. **B/C 会推翻 D4。** D4 明确决定"改造存量",M0 又刚刚证明那批脏数据(6 个僵尸 Pod、43 天监控历史)就是核心排障素材。**为了省事重建集群,等于把 M0 的最大产出扔掉**
2. **瓶颈根本不是"总量不够",是"分布不均"。** agent-1 空着 7G,ArgoCD 和 Jenkins **本来就不需要 GPU**,没有任何理由跑在 agent-0 上
3. **nodeSelector 是生产标准姿势**:按资源特征给工作负载选节点。这本身就是面试点

### 实施

1. `k3d cluster stop ew-cluster` ✅ **已完成**(2026-09-16,释放 ~811MB)
2. ArgoCD / Jenkins 的清单加 `nodeSelector: {node-role: cpu}`

### ⚠️ 实施中的修正(2026-09-16)

**原方案写的是 `kubernetes.io/hostname: k3d-ai-cluster-agent-1`,实施时发现应该改掉。**

排查雷 1 时读取 `Deployment/ollama` 的实际规格,发现集群**已经有现成的节点标签约定**:

| 节点 | 关键标签 | 角色 |
|---|---|---|
| `agent-0` | `nvidia.com/gpu: "true"` | GPU 节点 |
| `agent-1` | `node-role: cpu` | CPU 节点 |
| `server-0` | `node-role.kubernetes.io/control-plane: "true"` | 控制面 |

而 `Deployment/ollama` **已经在用 `nodeSelector: {node-role: cpu}`** —— 正是 D7 想做的事。

**改用 `node-role: cpu` 的三条理由**:

1. **沿用已有约定**,不引入第二套说法
2. **节点重建后名字会变,角色不会变**。k3d 集群重建时 agent 名字可能不同,"CPU 节点"这个角色是稳定的
3. 与集群里已有的工作负载保持一致

> **教训:先看现场有什么约定,再决定新增什么。** 用自己的方式解决问题不算错,但和已有写法并存就是自找混乱。

### 何时重新评估

如果 agent-1 也逼近上限——那时说明 WSL 的 19G 总量真的不够,需要减少组件或换机器。

---

## D8:PushPlus token 泄露的处理

- **日期**:2026-09-16
- **状态**:✅ 已决定
- **触发**:M0 安全发现

### 背景

M0 实测:明文 token 在**公开仓库**的 **4 个文件**、**3 个 commit** 里。

```
sre-lab-gitops/manifests/monitoring-stack/wechat-adapter.yaml
sre-lab-gitops/production/monitoring/wechat-adapter.yaml
sre-lab-gitops/k3s-ai-platform/environments/prod/monitoring/wechat-adapter.yaml
sre-lab-gitops/k3s-ai-platform/reference/baseline-manifests/monitoring-stack/wechat-adapter.yaml
```

**关键缓解因素:这个 adapter 从未部署**(集群里 0 个对象),所以没有正在运行的服务因此受影响,也没有服务中断风险。

### 考虑过的选项

| 选项 | 优点 | 缺点 |
|---|---|---|
| **A. 吊销 + Sealed Secrets,不清历史** | 零风险,代价小;旧 token 失效后留在历史里无害 | 历史里仍能看到失效 token |
| B. A + `git filter-repo` 重写历史 | 彻底干净 | 所有 commit hash 重写;需临时关分支保护;公开仓库强推有风险窗口 |
| C. 先记待办,M2 再处理 | 现在省事 | 期间 token 持续公开 |

### 决定

**A。**

### 理由

1. **凭证安全的第一原则是"让它失效",不是"让它看不见"。** 吊销之后,历史里那串字符就是一个无意义的字符串
2. **B 的代价与收益不成比例**。重写公开仓库的 3 个历史 commit,风险(强推失败、协作者混乱)大于收益
3. **C 不可接受**:token 虽然危害有限(能被用来给别人发消息),但"已发现却不处理"在安全上是不可辩护的

### ⚠️ 修订(2026-09-16):本决定的第 1 步已被推翻

**用户否掉了"吊销"。** 理由:项目已不再使用 PushPlus,该 token 事实上已是一串死凭证,吊销它的收益为零,而且会牵出一轮多余的账号操作。

> **取代本条的决策见 D11。**
>
> 本 ADR 保留原文不删 —— **决策日志的价值在于记录"当时是怎么想的",而不是只记录最终答案。** 这里保留是为了留下一个真实的判断失误样本:我当时按"标准安全流程"给出了吊销建议,没有先问"这个凭证还在不在用"。

### 实施(有严格顺序)

> ⚠️ **整节已作废,保留原文供对照。** 原方案建立在"吊销后发新 token"上,而 D11 决定不吊销;且接收端从 PushPlus 换成了企业微信机器人。新的实施步骤见 D11 / D12。

1. ~~用户登录 pushplus 后台吊销旧 token~~
2. ~~用户生成新 token(不要告诉我,也不写进任何文件)~~
3. 部署 sealed-secrets controller
4. ~~用户用 `kubeseal` 把新 token 加密成 `SealedSecret`~~
5. 删除 4 个明文文件
6. M2 的告警通道改为消费这个 Secret

**仍然有效的部分**:第 3 步(sealed-secrets controller,✅ 已完成,见 D12 前的部署)与第 5 步(**删除明文文件**——与吊销与否无关,文件本身该删)。

### 教训(写进 runbook)

**这次泄露的形态本身值得记录**:token 不是"不小心提交的",而是**被硬编码进一个 shell heredoc,再被复制成四份**。原始设计就有问题:

```yaml
image: python:3.9-slim
args:
- |
  pip install flask requests
  cat << 'PYEOF' > /app.py
  PUSHPLUS_TOKEN = "<此处原本是一串 32 位明文凭证,已脱敏>"
```

**三个问题叠加**:
1. **凭证硬编码进清单** — 清单是要进 Git 的,凭证不是
2. **启动时 `pip install`** — 依赖互联网、无版本锁定、Pod 重启就重装一遍
3. **内容重复四份** — 一份泄露 = 四份泄露

**正确姿势**:凭证走 Secret(再经 Sealed Secrets 加密),adapter 打成不可变镜像。

---

## D9:ollama 的 GitOps 定义与现场冲突的解法

- **日期**:2026-09-16
- **状态**:✅ 已决定
- **触发**:M1 审计雷 1

### 背景

| | GitOps 定义 | 集群现状 |
|---|---|---|
| 工作负载 | `StatefulSet/ollama` | `Deployment/ollama` |
| Service | `Service/ollama-service` ← **同名** | `Service/ollama-service` |
| Service | `Service/ollama-headless` | (无) |
| selector | `app: ollama` | `app: ollama` |

ArgoCD 会接管同名 Service,同时新建 StatefulSet。两边 Pod 都带 `app: ollama` → **一个 Service 选中两套 Pod**。

### 考虑过的选项

| 选项 | 优点 | 缺点 |
|---|---|---|
| **A. 改 GitOps 对齐现场** | 零风险,不碰生产 | GitOps 里少一个 StatefulSet 教学点 |
| B. 迁移到 StatefulSet | 有状态服务理论上更适合 | 需停机、重挂 PVC、过渡期双跑(agent-0 已 82%) |
| C. 暂不纳入接管 | 最安全 | GitOps 覆盖不全,面试故事弱 |

### 决定

**A。**

### 理由

1. **接管存量系统的第一原则:让 Git 描述现实,而不是让现实服从 Git。**

   集群里的 Deployment 已经稳定跑了 43 天,是**事实**;GitOps 里的 StatefulSet 从未部署过,是**设想**。用一个未经验证的设想,去替换一个稳定运行的事实,**方向和风险都反了**。

2. **顺序不能颠倒**:先接管(建立可见性和漂移检测)→ 稳定之后再谈迁移。一开始就"顺手把它改对",等于在一次变更里同时做两件事——**出问题无法归因**。

3. **这本身就是高价值面试点**:面试官问"接管存量时清单和现状不一致怎么办",能答出"先让 Git 对齐现实"的人不多,大多数人会本能地说"以 Git 为准"。

### 实施

`production/apps/ai-platform/ollama-stack.yaml`:

- `StatefulSet/ollama` → `Deployment/ollama`,字段对齐集群现状(副本数、镜像、资源、探针、selector)
- 删除 `Service/ollama-headless`(集群里不存在,也没有消费者)
- 保留 `Service/ollama-service`,确保 selector 为 `app: ollama`

### 何时重新评估

接管稳定、有了漂移检测之后,可以**单独**评估"Deployment → StatefulSet"的迁移,作为一次**独立的、可回滚的**变更。

### 副作用

`volumes` / `volumeClaimTemplates` 的差异需要逐项核对——Deployment 用的是 PVC + volumes,StatefulSet 用 `volumeClaimTemplates`。

---

## D10:Namespace 资源的接管方式

- **日期**:2026-09-16
- **状态**:✅ 已决定
- **触发**:M1 审计雷 2

### 背景

`apps/ai-platform/namespace.yaml` 声明了 `Namespace/ai-platform`,而 `ai-platform-app` 的 `syncPolicy` 是 `prune: true` + `selfHeal: true`。

**后果**:命名空间本身成了被 ArgoCD 管理的资源。一旦 `namespace.yaml` 从 Git 移除或改名,**ArgoCD 会 prune 掉整个 `ai-platform` 命名空间,连同里面所有工作负载和 PVC。**

### 决定

**从清单里删除 `namespace.yaml`,改用 ArgoCD 的 `CreateNamespace=true`。**

```yaml
syncPolicy:
  syncOptions:
    - CreateNamespace=true
```

### 理由

1. **声明式工具管"存在性",但不该管"删除边界"。**

   `Namespace` 是 cluster-scoped 的删除边界。把它交给 `prune: true`,等于在仓库里放一个"删一行 YAML 就删库"的开关。**声明式的价值是让期望状态可见,而不是让破坏性操作变得更容易触发。**

2. **这是 ArgoCD 官方推荐做法。** `CreateNamespace=true` 的设计意图正是:命名空间由 ArgoCD **按需创建**,但不纳入生命周期管理。

3. **`Destructive: prune` 的危险不在"它会不会删",在"它删的时候你不在场"。** 命名空间级联删除是不可逆的,且有几十秒的延迟——等你发现,已经来不及。

### 实施

- 删除 `production/apps/ai-platform/namespace.yaml`
- 在 `ai-platform-app.yaml` 的 `syncPolicy.syncOptions` 加 `CreateNamespace=true`
- 同样检查其余 5 个子 Application 的 `CreateNamespace` 配置

### 附带修正

其余子 Application(monitoring / loki / promtail / nginx-demo / sealed-secrets)的目标命名空间同样需要 `CreateNamespace=true`,否则首次同步会失败。

---

## D11:**不**吊销 PushPlus token(取代 D8 的吊销部分)

- **日期**:2026-09-16
- **状态**:✅ 已决定(**用户拍板**)
- **触发**:D8 的实施步骤 1 被推翻

### 背景

D8 里我给出的第一建议是"去后台吊销这个 token"。用户否掉了,原话:

> "不用吊销,我们已经不用 pushplus 了不是吗,干脆扔着不受影响,之前也是因为频繁无效告警加上项目当时完工集群不必维护,索性把告警打入黑洞。"

### 决定

**不吊销。** 同时**不再讨论**这个 token 的处置。

### 理由

1. **它已经不在任何调用路径上。** 项目早已不用 PushPlus,通道已废,这串字符在功能上等同于一串随机文本。**吊销一个没人用的凭证,收益是零**
2. **吊销不是一个孤立动作。** 它牵出一串后续:登录后台 → 找到应用 → 确认影响面 → 执行吊销。而 D8 的方案还要求"再生成一个新 token"——为了一个已经废弃的通道,做两轮账号操作
3. **它不构成实际威胁。** 危害上限是"别人能往我的个人 PushPlus 发消息",而那个账号本身已处于弃用状态

### 仍然执行的部分

**明文文件照样删。** 吊销与否和"文件该不该留在仓库里"是两个独立问题:

- 吊销解决的是"凭证还能不能用"
- 删文件解决的是"仓库里不该有凭证"

**后者不因前者作废而作废。** 删掉它们,顺带解决一个真实的维护问题:这四份文件是 `python:3.9-slim` + 启动时 `pip install` 的最坏范式(D8 教训节),本来就要换掉。

### 我的判断失误(要记住)

**我按"标准安全流程"给出了吊销建议,却没有先问一句"这个凭证还在用吗"。**

安全建议里有一条容易被忽略的前置:**处置的前提是"它还有价值"。** 对一个已经失效的凭证执行吊销,属于仪式性动作——看起来专业,实际只是在走流程。

> **教训:提安全建议之前,先问"这个资产现在是什么状态"。** 一个 43 天前就废弃的 token 和一个正在服务的 token,处置方案完全不同。

---

## D12:告警适配器选 `bougou/alertmanager-webhook-adapter`

- **日期**:2026-09-16
- **状态**:✅ 已决定(**用户认可**:"毕竟我们那个又老又旧不好用")
- **触发**:M2 接告警通道,需要把 Alertmanager 的 webhook 转成企业微信机器人格式

### 背景

仓库里现成的 `wechat-adapter.yaml` 有三个独立成立的问题(D8 教训节):启动时 `pip install` 不锁版本、必须可写根文件系统、源码藏在 Deployment 的 `args` heredoc 里。

### 考虑过的选项

| 选项 | 评价 |
|---|---|
| **A. `bougou/alertmanager-webhook-adapter`** | ✅ **选中**。Go / Apache-2.0,2026-08-31 仍有提交,v1.1.14(2026-08-30);支持 `weixin`(企微群机器人)/ dingtalk / feishu / slack;有官方镜像 + Helm chart |
| B. 修现有 Python adapter | ❌ 三个问题都得重写才能解决,重写完就是另一个项目了 |
| C. 自己写一个 | ❌ 属于典型的"本可以不用写";适配器是纯翻译层,没有业务特异性 |
| D. 其余社区 adapter | ❌ 全部停更 1~4 年 |

**选 A 的关键理由是它的活跃度**:这是能找到的**唯一近 30 天仍有提交**的中文告警适配器。告警适配器直接依赖上游 API(企微机器人接口),停更项目在接口变更时会直接静默失效。

### 附带发现:密钥边界变了

该项目的 token **走 URL query**(`?channel_type=weixin&token=...`),而这个 URL 要写进 Alertmanager 的 `webhook_configs.url`。于是:

```
SealedSecret → k8s Secret → 适配器读取        (原设想:密钥边界 = 一个 Secret)
SealedSecret → ALERTMANAGER 配置存储          (实际:密钥边界 = Alertmanager 的配置)
```

这**不比现状更糟**(Alertmanager 配置本来就在 Secret 里,不进 Git),但它把"密钥存在哪"这个问题**转移到了 Alertmanager 上**——

> **而本项目的 Alertmanager 配置恰好不在 GitOps 里**(见 D14)。所以"receiver 配置怎么进 GitOps"成了接通道的**前置问题**,必须先解决。

### 实施

尚未开始。前置条件(按顺序):
1. 拿到真实的企业微信**群机器人** webhook URL(见下)
2. 解决 D14 提出的"receiver 配置如何进 GitOps"
3. 部署适配器,封存 webhook
4. 端到端验证(真实收到一条消息)

### ⚠️ 关于 webhook URL 的一个易错点

用户曾提供过一个 URL 问我是不是 webhook:

```
https://work.weixin.qq.com/wework_admin/common/openBotProfile/24f12ca33810f4783566fe7a1fb0e5c921
```

**这不是 webhook。** 这是**管理后台的机器人详情页地址**,`24f12ca3...` 是后台内部的资源 ID,不是密钥。

真正的 webhook 长这样:

```
https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=<真实的 key>
```

**获取路径**:企业微信 → 目标群聊 → 右上角 `···` → 群机器人 → (添加后)复制 Webhook 地址。

**注意**:webhook URL 是**bearer 凭证**,拿到即等于拿到发消息权限,绝不能进 Git。

---

## D13:降噪方式 = 从源头关掉组件(而非静默或覆盖)

- **日期**:2026-09-16
- **状态**:✅ 已执行并实测验证
- **触发**:M2 降噪,告警数 **39 → 1**

### 背景

`KubeProxyDown` / `KubeSchedulerDown` / `KubeControllerManagerDown` 三条在 k3s 上永远 firing(原因见踩坑 P5:k3s 把这三个组件编译进单进程,而默认规则假设它们是独立 Pod)。

### 考虑过的选项

| 选项 | 评价 |
|---|---|
| **A. Helm values 关 `enabled`** | ✅ **选中**。一次砍掉 Service + ServiceMonitor + 空 target + **整组 PrometheusRule** |
| B. `defaultRules.disabled` 逐条禁 | ⚠️ 有效,但**保留空 target**。Prometheus UI 里仍是缺口 |
| C. Alertmanager 静默 / 黑洞路由 | ❌ 规则仍在求值(白烧 CPU);且实测**静默路由本身是失效的**(踩坑 P8) |
| D. 同名 PrometheusRule 覆盖 | ❌ **已实验证伪**(踩坑 P7):Prometheus 里同名规则**并存不覆盖** |

### 决定

**A。** 三个组件全部 `enabled: false`。

### 理由

1. **它解决的是根因,不是症状。** 真正的错误是"这些 target 在 k3s 上根本不该存在",关 `enabled` 修正的正是这个;静默只是假装它不存在
2. **副作用全部是正向的**:少 3 个 scrape target(省 Prometheus 资源)、少一整组规则(省求值开销)、少 3 个空 Service
3. **可逆且显式**:将来若换成 kubeadm 集群,把三行改回 `true` 即可

### 一个存放位置上的坑

values 文件放在 **`production/helm-values/`**,而**不是** `production/monitoring/`。

原因:`production/monitoring/` 在 `monitoring-app.yaml` 的 `source.path` 里,ArgoCD 会把该目录下**所有 YAML 当成 k8s 清单去 apply**。一个 Helm values 文件放进去,会变成"ArgoCD 尝试创建一个不存在的 kind"。

> **规律:GitOps 监视目录里不能放非清单文件。** 目录边界就是语义边界。

### 实测验证

| 检查项 | 结果 |
|---|---|
| 渲染结果里三个组件的任何资源 | 0 个 |
| 渲染结果里三条 alert 名 | 0 次 |
| 集群里对应 Service / ServiceMonitor / PrometheusRule | 已全部清除 |
| **Prometheus 实际告警数** | **39 → 1**(仅剩 `Watchdog`,设计上永远 firing) |

---

## D14:监控栈保持 Helm 管理,不进 GitOps

- **日期**:2026-09-16
- **状态**:✅ 已决定
- **触发**:M1 接管存量时梳理"哪些该交给 ArgoCD"

### 背景

存量集群里 `monitoring` 是 **Helm release**(`kube-prometheus-stack-87.19.1`,rev 4),而 `ai-platform` 是**手工 apply** 的。两种管理方式需要两套接管策略。

### 决定

**混合模式**——监控栈"主体"由 Helm 管理,不交给 ArgoCD;但**补充对象**走 GitOps。

| 层 | 管理方式 | 内容 |
|---|---|---|
| 主体 | **Helm**(手工 upgrade) | kube-prometheus-stack 的全部内置资源:Prometheus / Alertmanager / Grafana / node-exporter / kube-state-metrics / 默认规则 |
| 补充 | **ArgoCD**(`monitoring-app.yaml`,监视 `production/monitoring/`) | cluster-alerts / nginx-monitor / ollama-exporter / ingress / 适配器 等后加的东西 |

### 理由

1. **避免双重管理。** 同一个对象被 Helm 和 ArgoCD 同时管,两边都会认为对方"漂移"了,互相 reconcile——这是 GitOps 落地时最常见的冲突来源
2. **Helm chart 的 values 是这类栈的天然接口。** kube-prometheus-stack 有 800+ 个配置项,拆成 k8s 原生清单会失去 chart 的升级路径
3. **补充对象本来就是清单形态**,且经常变动,适合 GitOps

### 代价(要诚实标注)

**这个分界不是免费的**,它引入了一个持续存在的风险:

> **Alertmanager 的 receiver 配置属于"主体",在 Helm values 里 —— 也就是说它在 GitOps 之外。**

后果很具体(见 D12):接告警通道时,密钥(webhook URL)会落到一个**不在 Git 里、也没有版本历史**的地方。

**这是 D12 里那个"前置问题"的根源。** 可能的解法(未定):
- 把 Alertmanager 配置整体迁进 GitOps(代价:要接管 chart 的一个子部分)
- 用 `alertmanager.config` 引外部 Secret(需要解决"Secret 怎么进 Git")
- 接受这个边界,用 SealedSecret 手工维护并文档化

**M2 接通道之前必须先把这条定下来。**

---

## D15:告警接收端改用邮件(取代 D12 的适配器方案)

- **日期**:2026-09-16
- **状态**:✅ 已实施并实测验证
- **触发**:企微群机器人方案被物理阻断,重新评估"最小可用接收端"

### 背景

D12 选定了 `bougou/alertmanager-webhook-adapter` 作为企微告警适配器。但它有一个前置条件:**必须先有一个群,才能建群机器人。** 用户用的是个人版企微,**无法创建群聊**,因此拿不到 webhook key。这条路径在物理上走不通,不是配置问题。

### 考虑过的选项

| 选项 | 结论 |
|---|---|
| 企微群机器人 | ❌ 个人账号无法建群,拿不到 key |
| 企微应用消息(`weixinapp`) | 需要企业管理员权限创建自建应用,个人版同样受限 |
| PushPlus 等第三方推送 | 已有 token,但要经第三方服务器中转;D11 决定不吊销它,也不扩大其使用面 |
| 钉钉 / 飞书群机器人 | 同样需要先建群或建组织,注册成本高于收益 |
| **邮件(SMTP)** | ✅ 不需要群、不需要组织、不需要第三方服务 |

### 决定

**用邮件作为告警接收端。** `to` / `from` / `authUsername` 均为 `599512147@qq.com`,`smarthost` 为 `smtp.qq.com:465`(`forceImplicitTLS: true`),凭据由 SealedSecret 提供。

### 理由

1. **它是这个环境里唯一"零前置"的通道。** 企微要群、钉钉要组织、PushPlus 要过第三方——邮件什么都不要
2. **密钥是原生 SecretKeyRef。** `AlertmanagerConfig.emailConfigs.authPassword` 直接就是 `SecretKeySelector`,配合 Sealed Secrets,D12 里那个"token 会不会落进 Git"的问题**从设计上就不存在了**。(对比:`webhookConfigs.urlSecret` 也能做到同样的事——那是 D12 走的弯路)
3. **不需要额外部署适配器。** 省掉一个 Deployment、一份镜像、一个要跟踪上游更新的第三方项目
4. **面试口径更干净。** "用 Alertmanager 原生 email receiver + SealedSecret 接通道"比"我装了一个社区适配器"更能体现对工具边界的理解

### 代价(要诚实标注)

1. **邮件不是即时通道。** 手机邮件推送延迟显著高于 IM,不适合 P1 级故障的实时响应。真上生产,邮件通常只做**兜底与留痕**,IM / 电话才是主通道
2. **邮件没有天然的限频与聚合。** 这次靠在 Alertmanager 侧做聚合(`groupWait: 30s` / `groupInterval: 5m` / `repeatInterval: 4h`)来替代企微那套"20 条/分钟"的硬约束
3. **QQ 邮箱 SMTP 有外部配额**,超限会被限制;本地实验量级无影响

### 实施

**第一步必须是改 `alertmanagerConfigMatcherStrategy`。**

`AlertmanagerConfig` 是**命名空间级** CRD,而 prometheus-operator 默认的 `OnNamespace` 策略会往它生成的**每一条子路由**注入 `namespace = "<CR 所在命名空间>"` 匹配器。我们的 CR 在 `monitoring` 命名空间 —— 不改这个设置的话,**只有带 `namespace=monitoring` 标签的告警能匹配到邮件路由**,其余全部回落到根路由的 `null` receiver。这是一个**完全静默的黑洞**,不报任何错。

所以在 `production/helm-values/monitoring-values.yaml` 里加了:

```yaml
alertmanager:
  alertmanagerSpec:
    alertmanagerConfigMatcherStrategy:
      type: None
```

**其余步骤**:

| 步骤 | 内容 |
|---|---|
| 1 | `monitoring-values.yaml` 加 `alertmanagerConfigMatcherStrategy: None`,`helm upgrade` |
| 2 | 写 `production/monitoring/alertmanager-config.yaml`(`AlertmanagerConfig`,route + email receiver) |
| 3 | 用 `kubeseal` 生成 `production/monitoring/sealed-alertmanager-smtp.yaml`(strict scope,name=`alertmanager-smtp`,ns=`monitoring`) |
| 4 | **先单独 apply SealedSecret**,确认 Secret 先于 Alertmanager 引用它而存在 |
| 5 | 再 apply `production/bootstrap/monitoring-app.yaml`,由 ArgoCD 接管 |

**第 4 步的顺序不是洁癖。** `AlertmanagerConfig` 引用一个不存在的 Secret 时,operator 生成的配置里 `auth_password` 解析失败;而 **Alertmanager 对配置加载失败的处理是保留上一份配置**——表现是"改了但没生效",极难定位。

### 验证(三层各自独立取证,全部为实测)

| 层次 | 取证方式 | 结果 |
|---|---|---|
| 配置生成 | 解出 `alertmanager-monitoring-kube-prometheus-alertmanager-generated` 里的 `alertmanager.yaml.gz` | 子路由 `receiver: monitoring/sre-lab-alerting/email`,**无注入的 namespace 匹配器**;`auth_password` 已解析 |
| 通道可达 | `alertmanager_notifications_total{integration="email"}` | **2** |
| 投递失败 | `alertmanager_notifications_failed_total{integration="email",reason=~".*"}` | **全 0** |
| 端到端 | 向 v2 API `POST /api/v2/alerts` 注入真实告警 | HTTP 200 → 进入 `active` → 触发 1 次投递 |
| **人工确认** | 用户查看 `599512147@qq.com` 收件箱 | ✅ **收到** |

**最后这一行不能省。** 前三层共同的极限是"SMPT 服务器**接受了**这封信"。"SMTP 接受了"和"进了收件箱"之间还隔着垃圾邮件过滤、灰名单、域名信誉——而这一段的失败对发送方是**完全静默**的:返回 250、计数器加一、`failed_total` 保持 0,邮件却躺在垃圾箱里。**链路的最后一跳如果是人,就只能由人来验证。**

**为什么用 `notifications_total` 而不是看日志**:Alertmanager 默认 `log.level=info`,**成功的通知不写日志**,只有失败才写。所以"`kubectl logs` 里没有 error"完全不能证明消息发出去了——那一刻它可能一条都没发。计数器是这里唯一可信的证据。

### 何时重新评估

拿到企业内部权限(能建群 / 能建自建应用)时切回 IM 通道,邮件降级为兜底。

---

## D16:镜像仓库选型 —— 本地 k3d registry(而非 GHCR)

- **日期**:2026-09-16
- **状态**:✅ 已决定并跑通
- **触发**:M3 需要一个可供集群拉取的镜像仓库

### 背景

原计划用 **GHCR**(GitHub Container Registry)。但被一个很具体的事卡住了:

> **GHCR 的经典 PAT(classic personal access token)迟迟没拿到。**

排查到的细节:`gh` CLI 拿到的 OAuth token **只有 `write:packages`,没有 `read:packages`**,于是 `docker login` 直接被:

```
DENIED
```

### 考虑过的选项

| 选项 | 评价 |
|---|---|
| **A. 本地 `k3d registry create`** | ✅ **选中** |
| B. 继续等 GHCR PAT | ❌ 被外部流程阻塞,且不是技术问题,无法预估何时解开 |
| C. 公网私有 registry 服务 | ❌ 引入第三方依赖与账号成本,对"本地可复现实验"没有增益 |

### 决定

**用 `k3d registry create` 起本地仓库(GHCR 留作第二站)。**

### 理由

1. **不被外部 token 阻塞。** 上面那个 `DENIED` 的根因是**权限范围**问题,不在本项目能控制的范围里。继续等它,是把项目进度押在一个外部流程上 —— **一个实验要用到的组件,不该被它自己的认证流程卡住**
2. **对 K8s 来说它就是真实私有仓库。** 认证、tag、拉取语义**和企业内网 Harbor 完全一致** —— 也就是说,本项目练到的"配私有仓库、给 containerd 加 `certs.d`、处理 HTTP/HTTPS 不一致"(见踩坑 P16)**是真实技能,不是玩具**
3. **自包含、可复现。** 本地仓库随 k3d 一起起、一起清,没有外部状态残留 —— 契合 D4 选定的"可重复 + 保留真实状态"路线

### 代价(诚实标注)

- **本地 registry 没有访问控制**,任何能访问 `localhost:5111` 的东西都能推送/拉取。仅适用于本机实验环境
- 集群重建后仓库内容需要重建(镜像不在,但仓库本身由 `k3d registry create` 一条命令恢复)

### 何时重新评估

拿到 GHCR 的 `read:packages` 权限后,把 GHCR 作为"第二站"接上 —— 那时本项目就有**两条镜像分发路径**可对照(本地 registry = 内网私有仓库,GHCR = 公网托管),这本身是更好的面试素材。

---

## D17:GitOps 写回用**仓库级 deploy key**(而非 Jenkins 原生凭据)

- **日期**:2026-09-16
- **状态**:✅ 已实施(含两处诚实边界)
- **触发**:M3 Jenkins 要向 GitOps 仓库写回镜像 tag

### 背景

**GitOps 写回必须要有 git 写凭据,这一条绕不过去。**

D2 定的分工是 Jenkins 做 CI、ArgoCD 做 CD。但"CI 构建出新镜像后,把新 tag 写回 GitOps 仓库"这个动作,**需要一个能 push 到该仓库的身份** —— ArgoCD 是 pull 模型,它不会替 Jenkins 做这件事。

### 考虑过的选项

| 选项 | 评价 |
|---|---|
| **A. 仓库级 deploy key + K8s Secret 挂载** | ✅ **选中** |
| B. Jenkins 原生 Credentials(`GitSSHUserPrivateKey`) | ⚠️ 尝试过,**REST API 创建返回 500**,时间原因改用挂载 |
| C. 个人账号 PAT / 长期 SSH key | ❌ 权限范围是整个账号,对"一个仓库的写回"来说**过宽** |

### 决定

**生成专用 ed25519 密钥对:**

- **公钥**注册为该仓库的 **deploy key**,**`read_only=false`**(= 可写)
- **私钥**以 **K8s Secret** 形式挂进构建 Pod 的 git 容器,挂载路径 `/root/.ssh/id_ed25519`,`defaultMode: 0400`

### 理由

1. **deploy key 的权限锁死在单个仓库。** 它**只能**读写这一个仓库,拿不到账号下别的东西 —— 这是 GitHub **专门为 CI 设计**的机制,权限边界和需求正好对齐
2. **对比 C:一个 PAT 泄露 = 整个账号泄露。** 一个 deploy key 泄露 = **一个仓库**泄露。**凭证的权限范围应该等于它要做的事的范围** —— 不要大于
3. **用 K8s Secret 挂载而不是塞进环境变量**:私钥以**文件**形态出现(`0400`,只读),符合 SSH 对私钥文件权限的要求,也避免它出现在 `kubectl describe pod` / 进程 env 里

### ⚠️ 诚实边界(两条,都是待办)

**边界 1:当前是普通 K8s Secret,没有走 Sealed Secrets 封存。**

> **集群里没装 `kubeseal` 二进制**,所以这次是直接创建 Secret 的。

后果很具体:**k3d 重建后这个 Secret 会丢,需要重新创建。** 这直接违背了 D4 选的"可重复重建"路线 —— 重建集群之后,CI 会因为找不到私钥而 push 失败,而**失败点离根因很远**(表现为"git push 报错",而不是"Secret 不存在")。

**待办:把 kubeseal 补上,把这份 Secret 封存进 GitOps 仓库。**

**边界 2:没有走 Jenkins 原生的 Credentials 机制。**

尝试过用 Jenkins REST API 创建 `GitSSHUserPrivateKey` 类型凭据,**返回 500**,时间原因改用挂载。两条路的差异:

| | 挂载 K8s Secret | Jenkins 原生 Credentials |
|---|---|---|
| 凭据存在哪 | **K8s**(集群里) | **Jenkins**(controller 上) |
| 谁能用 | 只有挂了它的 Pod | 任何有权限的 Job |
| 轮换 | 改 Secret + 重启 Pod | Jenkins 界面里改 |

**挂载这条路的权限边界更窄**(只有构建 Pod 能看到私钥),代价是**轮换麻烦**。当前选择它是因为它先跑通了。

### 何时重新评估

1. 补上 `kubeseal` 之后 —— Sealed Secrets 封存私钥,解决边界 1
2. 若之后需要多个 Job 共享该凭据 —— 重新评估是否切回 Jenkins 原生 Credentials(边界 2)

---

## D18:Git 服务**留在集群外**,用集群内的 Service/Endpoints 提供声明式解析

- **日期**:2026-09-17
- **背景**:2026-09-16 宿主重启后,ArgoCD 6 条 Application 全部 `Unknown`。直接死因是集群内 `gitea` 解析不到(P25),而那个解析靠的是**宿主机容器运行时的副作用**,不在 Git、不在 k8s、不在任何配置文件里。这是"修 10 个洞"这件事第一次被证明**根因在架构层,不在流水线层**。

### 考虑过的选项

| 选项 | 内容 | 判定 |
|------|------|------|
| ① | 把 Gitea **搬进集群**(StatefulSet + PVC + Service),由 ArgoCD 自管 | ❌ 否决 |
| ② | 改 CoreDNS `NodeHosts`,写死 `gitea`→IP | ❌ 否决 |
| ③ | ArgoCD 读路径**改指 GitHub**(实测集群内 git-http 通) | ❌ 否决 |
| ④ | Gitea 容器**留在集群外**,但在**消费方命名空间**各放一份 selector-less Service + 手工 Endpoints | ✅ **选定** |

### 逐条否决的理由

**① 把 Git 搬进集群** —— 反直觉,但结论明确:**集群一灭,你连"从 Git 恢复"都做不到**。Git 服务放进被它管理的集群 = **更深的自锁**。这条推翻了我自己在上一轮给出的选项,已在 P24/P25 里留痕。

**② 写死 NodeHosts** —— `NodeHosts` 是**节点级**配置,不是集群内的声明式对象;而且硬编码 IP 引入新的失效源(容器重建换 IP 就断)。它解决不了 R2 的本质。

**③ ArgoCD 读 GitHub** —— 集群内**确实**能访问 GitHub(实测 `github.com:443` 通、`git-upload-pack` 返回 200)。但本机到 `github.com` 的通道**被 SNI 阻断过**(间歇性)。把"集群收敛的**唯一真源**"押在一条会抖的跨境链路上,等于用**一个新的、不可控的依赖**换掉一个可控的依赖。**不做。**

**④ 选定** —— 在 `argocd` / `jenkins` 各放一份 Service + Endpoints,让 `gitea` 由 **k8s 对象**解析。

### 决定

**选 ④**:Gitea 容器留在集群外(D18 维护了 R1「Git 必须能脱离集群存活」这个正确结论),但把它的**名字解析**改由集群内的声明式对象提供。

### 理由

R2 的本质是"解析依赖跨层副作用"。④ 用**集群内的声明式对象**直接消灭它,同时不动 R1。

### 后果与代价(诚实标注)

1. **Endpoints 里硬编码容器 IP** → 容器重建换 IP 就失联,而且**比原来更隐蔽**:k8s DNS 会抢先返回一个死端点,不再是 `NXDOMAIN` 那种一眼能看出的错。
   **缓解(两层)**:容器创建时用 `--ip` 固定 IP;`gitea_up.sh` 每次跑都按**实际容器 IP** 重放这两个对象并核对。
2. **每个消费方命名空间一份**,是我**实测**到的 k8s 短名语义决定的冗余(短名只在同命名空间解析,见 P24),不是设计冗余。

### 何时重新评估

1. 引入第二个 git 服务 / 多集群时 → 消费方改用**全名** `gitea.default.svc.cluster.local`,即可只留一份
2. 若 GitHub 通道被证明稳定(连续数周无阻断)→ 可重新评估 ③,把自托管 Git 从关键路径上彻底摘掉

---

## D19:**不拆双仓**,靠"路径不相交 + Trigger Guard 兜底"阻断自激循环

- **日期**:2026-09-17
- **背景**:同一个仓既放源码(`ci/`、`03-ollama-exporter/`)又放 GitOps 清单(`sre-lab-gitops/`)。CI 写回清单 → 再次触发 CI → 自激循环。P19 已经真发生过一次("修好了触发,流水线开始无限自我触发")。

### 考虑过的选项

| 选项 | 内容 | 判定 |
|------|------|------|
| ① | **拆双仓**(源码仓 + 清单仓) | ❌ 本次否决 |
| ② | 同仓 + 路径闸门 | ✅ **选定** |

### 决定

**保留 ②**。不拆仓。

### 理由

- **实测基线构建 #23 证明闸门有效**:
  ```
  本次推送涉及的文件: sre-lab-local/03_runbook/02_ArgoCD_运行手册.md
  本次推送未涉及 ci/ 或 03-ollama-exporter/, 跳过构建(阻断 CI 写回配置仓造成的自激循环)
  Finished: SUCCESS
  ```
- 写回路径 `sre-lab-gitops/` 与构建路径 `ci/` + `03-ollama-exporter/` **完全不相交**。所以挡住循环的是"**路径不相交**"这个更简单的事实,`Trigger Guard` 只是**第二道防线**。
- 拆仓要从零重建仓库、迁移清单、改 7 处 ArgoCD `repoURL`、改 Jenkins 任务 SCM、重配 webhook。**收益(删掉一个 stage)远小于代价与风险。**

### 代价(诚实标注,这条很重要)

> **没有从结构上消灭循环,只是让循环无害。**

风险是**潜伏**的:如果将来有人把构建触发路径扩大(比如把 `sre-lab-gitops/` 加进去),循环**立刻回来**,而且表现为"推一次构建两次"——**这正是 P19 已经发生过一次的事**。

也就是说,② 的守护依赖"**有人记得别动那个列表**"。这是一笔利息很低的债,但它确实是一笔债。

### 何时重新评估

1. 构建触发路径需要扩大时(**必须同时重新评估本决定**)
2. 出现第二个消费这份清单的应用 / 需要独立发布节奏时

---

## D20:CI 单元测试用**标准库 `unittest`**,不引入 pytest

- **日期**:2026-09-17
- **背景**:修 10 个洞里的 #1(流水线无测试:原 `Lint` stage 只做 `python -m py_compile`)。构建 agent 的 `lint` 容器是**裸 `python:3.11-slim`**,没装 pytest。

### 考虑过的选项

| 选项 | 内容 | 判定 |
|------|------|------|
| ① | 测试 stage 里 `pip install pytest -i 清华镜像` | ❌ 否决 |
| ② | 用标准库 `unittest` | ✅ **选定** |

### 决定

**选 ②**:测试代码写成 `unittest.TestCase`,用 `python -m unittest discover` 跑。

### 理由

1. ① 每次构建多一次**跨境取包**,等于给"绿"引入一个**与代码质量无关的失败源**——在它坏掉的那天,你会以为是自己的代码坏了。
2. ② **零依赖、零网络**,镜像原样可用。**测试要跑得快、要不依赖外部世界,否则它会被绕过。**
3. 关键:**`unittest.TestCase` 写的测试同时能被 pytest 跑**。将来要换,迁移成本是零。

### 代价(诚实标注)

断言表达力弱于 pytest:没有 fixture 参数化、没有 `pytest.raises` 的简写、`-v` 输出更啰嗦、没有内建覆盖率。

### 何时重新评估

测试需要 fixture 参数化 / 覆盖率报告 / 并行执行时 → 在镜像里预装 pytest(而不是每次构建装)。

---

## D21:Jenkins 自身的插件缓存(emptyDir)**这一次不修**

- **日期**:2026-09-17
- **背景**:#9「构建无缓存」有**两处同源**问题——buildah 的 `/var/lib/containers`,和 Jenkins 的 `plugin-dir` / `jenkins-cache`,都是 `emptyDir` 当缓存。本次只修了 buildah 那处。

### 决定

Jenkins 那处**推迟**,走独立窗口。

### 理由

它要改 Helm values **并重启 Jenkins**,而重启 Jenkins = init 容器重下全部插件 = **分钟级不可用**(`03_runbook/03_Jenkins_运行手册.md` 第四节)。

把这个代价**叠加在本次变更上**,会让"构建失败"的归因同时面对两个变化:**新代码** 和 **新插件状态**。

> **归因难度的成本,高于省下的那点时间。**

`03_Jenkins_运行手册.md` 第四节自己写过的纪律:**"任何需要重启才生效的事,要趁早做——不要等到出问题才重启。"** 执行本决定时就按这条来。

### 代价(诚实标注)

**这笔债还在。** 每次 Jenkins 重启仍然是分钟级。而且它**已经咬过一次**——P26 里的 init `CrashLoopBackOff`(半截下载状态留在 emptyDir 里,重试 8 次都过不去)。

### 何时执行

下一个**不以交付为目的**的窗口。做完立刻验证一次重启耗时,把"分钟级"这个数字更新成实测值。

---

## D22:WSL 里的 `~/projects/sre-lab` 是**编辑源**,不是发布源——**不**强行与远端对齐

- **日期**:2026-09-17
- **背景**:端到端验证收尾时核查工作副本,发现:

```
$ git rev-parse --short HEAD
dea2f70                        ← 远端已经是 3858e32
$ git status --porcelain
 8 个文件 M(已修改)+ 6 项 ??(未跟踪)+ 2 个文件 M(只有 mode 变化)
$ git status -sb
## main...origin/main          ← 没有 behind 标记
```

看起来像"WSL 里有一批没发布的工作",需要"同步一下"。**核查之后,这个判断不成立。**

### 核查过程(先量,再决定)

1. `git diff --numstat` 逐文件看:`cluster-health-check.sh` 和 `重建.sh` 是 **`0 增 / 0 删`** —— 纯 **mode 变化**(9p 文件系统的权限位表现),**没有任何内容差异**。
2. 剩下的 8 个 M + 6 项 ??,**内容逐条等于**已经提交进 `497b857` 的那 15 个文件。
3. 结论:**WSL 副本里没有任何"独有内容"**。它不是"分叉",只是"没往前走"。

### 决定

**不**在 WSL 内做 `git pull` / `git reset --hard`,也**不**改变既有的落盘通道(改 UNC 上的文件 → 复制进 Windows 侧克隆 → commit → push)。保持 WSL 副本作为**编辑源**。

### 理由

1. **零内容丢失**——所有独有内容都已进 `497b857`;
2. **"对齐"没有收益**——下一次编辑照样是"改 UNC 文件 → 复制 → push",工作副本的 git 头在哪不参与这条通道;
3. **反向代价明确且更大**——一旦在 WSL 里 `git pull`,**15 个已修改/未跟踪文件会与入站改动正面对撞**,而冲突解决要在 9p 文件系统上做,比在 Windows 侧复杂得多。**为了一个好看的状态去制造一次冲突,是负收益。**

### 代价(诚实标注)

- WSL 副本的 `git status` 会长期是"脏"的;
- 它**不 fetch**,所以 `status -sb` **不会显示 behind** —— 看它的人会误以为"这里有未发布的工作"。
- → 已在 `08_重建/README.md` 写明这条读取注意事项,避免下一次误判。

### 何时改变

等"编辑源"和"发布源"合成一条通道之后(例如 WSL 内能直接跑 `git` 时),再统一两者。**在那之前,这个"脏"是设计的一部分,不是待办。**

---

## D23:数据层**搬进集群**,放弃"把集群外的 PG 投影进来"

- **日期**:2026-09-17
- **关联踩坑**:P33

### 考虑过的选项

| 选项 | 做法 | 代价 |
|---|---|---|
| A. 投影外部库 | 无 selector 的 Service + **手工 Endpoints**,指向 WSL docker 里的 `pg-platform`(172.18.0.7) | 清单里的 Endpoints **不被 ArgoCD 纳管**(默认 `resource.exclusions` 排除)。要么改平台全局配置,要么承认"清单里有一部分不由 ArgoCD 管" |
| B. 集群内部署 | PG 作为 `Deployment` + **带 selector 的普通 Service**,Endpoints 由控制面自动生成 | 集群停 = 库停;`ReadWriteOnce` 卷 → 更新策略必须是 `Recreate` |
| C. 改 `argocd-cm` 去掉排除 | 让 Endpoints 可纳管,保留投影写法 | 动的是**全局豁免**;且 Endpoints 由控制面高频改写,纳管会造成持续 OutOfSync 抖动 |

### 决定

**选 B。**

### 理由

1. **A 的必要性来自沉没成本,不来自需求。** "投影"之所以看起来必要,只是因为 PG 已经在 WSL 里跑好了。但需求侧要的是"数据层能被声明式交付",这一点 B 做得更彻底。
2. **C 是拿全局配置换一个局部的、非必要的写法。** 而且换来的是"纳管一个高频改写对象"的新麻烦。
3. **B 顺带修掉一个更隐蔽的问题**:库与集群的生命周期一致。A 之下,`kubectl apply` 成功不等于库真的在;两端各自独立地起停,谁也不知道对方在不在。
4. **B 的清单全是 ArgoCD 原生对象**,资源列表里每一项都有,`sync=Synced` 名副其实 —— 这让"Synced"这个词重新变得可信。

### 代价(诚实标注)

- **集群停 = 库停**。调试链路变长:以前查库可以直接连 `pg-platform`,现在要先确认集群在跑。
- 多了一个 **`Recreate` 约束**:`ReadWriteOnce` 卷不能被两个 Pod 同时挂,所以滚动更新会损坏数据 → 策略必须写死 `Recreate`,**并且这一条不能靠人记,要写进清单注释**。
- WSL 里那个 `pg-platform` 容器**仍在跑,但已不是交付路径的一部分**。这形成了一个"看起来在干活其实不在链路里"的残留物 —— 属于本项目主题(静默失效)的又一次实例。

### 何时重新评估

出现"库需要独立于集群伸缩/升级"的真实需求时(例如要接一个集群外的托管库)。那时正确形态是 **ExternalName / 服务投影 + 明确承认它不由 ArgoCD 纳管**,而不是假装它在 Git 里。

---

## D24:首个 admin API Key 用**带外一次性 Job** 引导,不开放"无鉴权引导端点"

- **日期**:2026-09-17
- **关联踩坑**:P34

### 问题(鸡生蛋)

`POST /api/v1/keys` **自身**就要 `RequireAdmin`。而 `APP_ENV=production` + `AUTH_REQUIRED=true` 下,**没有任何 HTTP 路径能签出第一把 key**。

这不是本项目的 bug,而是"API Key 自助签发"这件事的**必然后果**:签发入口必须有一条不经过 HTTP 的路径,否则第一个管理员永远进不来。

### 考虑过的选项

| 选项 | 做法 | 为什么没选 |
|---|---|---|
| A. 带外一次性 Job | 一个 `ci/bootstrap_admin.py`,**复用服务自己的 `ApiKeyService`**,直接往库里落一条 `admin` key | —— |
| B. 无鉴权引导端点 | 加一个只在"库里没有任何 key"时放行的端点 | 引入一个**状态相关的鉴权例外**。它的判据(库里没 key)本身要查库,而它又是未认证可访问的 → 变成了一个可在生产打到的匿名写入口。**用一个永久的攻击面换一次性的便利** |
| C. 环境变量注入初始 key | 启动时若库里没 key,从 env 读一把固定 key 落库 | 等于把"第一把 key"变成**配置**,它会长期留在 Helm values / 部署清单里,且轮换要重启。而且"启动时偷偷建 key"是个隐式副作用 |
| D. 手工 `psql` INSERT | 运维手工插入 | 不可复现、不可审计,且**哈希规则要人手算** —— 迟早和服务的实现分叉 |

### 决定

**选 A。** 并且引导脚本**必须 import 服务自己的实现**(`from src.service import ApiKeyService`),不允许自己拼 SQL 或自己实现哈希。

### 理由

1. `key_hash = sha256(secret_pepper + plaintext)`、`prefix = plaintext[:8]` 是**服务运行时的契约**。复制一份到引导脚本里,将来只要 pepper 的用法动一下(换拼接顺序、加分隔符、改算法),脚本照样能插入一行"看起来正常"的记录,**但服务永远校验不过 —— 而且不报错,只是 401**。所以单源是硬要求,不是风格偏好。
2. 幂等键取 `name == 'bootstrap-admin'`,重复执行不会堆一串 key。
3. 一次性 Job 的权限面是**可回收的**:跑完即删,不存在一个常驻的引导入口。

### 代价(诚实标注)

- **明文 key 只有第一次能看到。** 库里只存哈希,不可逆。丢了只能"先吊销、再换名字重新引导"。
- **为了拿到明文,它必须经过 Job 日志**,而 Job 日志是一种可读的落盘载体。本项目的处置是:**采集后立即删除 Job**(日志随之消失)、明文只留一份**不在任何 git 仓库里**的本地文件、所有记录里出现的明文一律抹除。**这是缓解,不是消除** —— 真正的生产应该写进 Secret / 外部密钥管理,而不是打印。
- 引导脚本不在镜像里(Dockerfile 只 `COPY src`),得靠 ConfigMap 挂进去。这是有意的:不让一次性运维脚本进运行镜像。

### 何时重新评估

引入外部身份提供方(OIDC / SSO)之后,"第一个管理员"应该由**身份系统**解决,这条路就该删掉。

---

## D25:把 **schema 迁移纳入 CI**,且排在构建之前

- **日期**:2026-09-17

### 考虑过的选项

| 选项 | 做法 |
|---|---|
| A. 迁移进 CI(选中) | `Migrate` stage 在 `Build` 之前,用 db 仓的 `migrations/001_init.sql` 应用到交付库,幂等 |
| B. 上线时手工跑 | 部署前后人工 `psql -f` |
| C. 迁移做成独立 Job / 审批阶段 | 生产上的正确形态,但需要一个"谁批准"的机制 |

### 决定

教学与验证阶段选 **A**,并在文档里**明确标注它是教学选择而非生产方案**。

### 理由

1. **`ai-infra-platform-db` 是 schema 的唯一契约来源。** 让流水线在部署前把这份契约应用到目标库,"代码"与"表结构"才在同一根时间线上。
2. **人手跑迁移迟早会错位**,而且是那种「上周五改了字段、这周三才发现服务起不来」的错位 —— 因为报错发生在**运行时**,不在部署时。
3. **放在构建之前有明确语义**:迁移失败就不该让新镜像上线。于是**"流水线绿了"就等价于"代码 + 表结构这一对是一致的"**。
4. 幂等键取"库里的实际状态"(`schema_migration` 表存在即跳过),而不是脚本里的标记位 —— 因为流水线可能重跑,而重跑时唯一可信的就是库自己的状态。

### 代价(诚实标注)

- **CI 能写生产库,这是事实。** 生产上迁移应该是**独立的一次性 Job,带审批**,而不是塞在构建流水线里。
- 当前只有一版迁移,`001_init.sql` 也是"只增不改"的约定。**这个约定没有机器校验** —— 改一个已发布的迁移文件不会被拦住。真正的工具(Flyway / Atlas)会存校验和来防这件事,我们没做。
- 迁移的**回滚**没有方案,只有"往前再写一版"。

### 何时重新评估

有第二个环境、或迁移开始带数据变更(不只是 DDL)时 —— 那时它必须从流水线里搬出来。

---

## D26:清单里的镜像 tag **归 CI 管**,人工只落一次初值

- **日期**:2026-09-17

### 决定

`sre-lab-gitops/production/apps/ai-platform/api.yaml` 里 `image:` 那一行的 tag 由 `platform-ci` 的 `Update GitOps` 阶段 `sed` 改写。人工**只在首次落地时**写一个初值。

### 理由

让"当前跑的是哪个 commit"这条信息,由**唯一的写入者**产生。两个写入者(人和 CI)必然分叉,而且分叉是静默的。

### 代价(诚实标注)

- **手工改 tag 会被下一次构建覆盖。** 这一点必须写在清单头部注释里,否则接手的人会以为手改有效。
- 更隐蔽的代价:**人工编辑源(`_gitops_stage/`)会在 CI 写回后变旧。** 如果下一次人工推送直接覆盖清单仓,就会把 tag **倒退**回去 —— 一个"人无意中把生产版本降级"的路径。

### 配套纪律(靠机制不靠自觉)

`push_gitops.py` 的动作顺序是**先 `git clone` 远端(拿到含 CI 写回的最新状态)→ 再覆盖文件 → 再提交**。所以远端已有的 CI 提交**不会被回退**。这条纪律落在脚本里,不落在人的记忆里。

### 何时重新评估

引入多环境后,"哪个环境的 tag 由谁写"需要重新定义 —— 那时可能要把 `Update GitOps` 改成"提交一个 PR 到环境清单",由人来合。

---

## D27:前端仓(第 1 层)接入流水线:**照 platform-ci 的形状,但砍掉两节、加一节**

**背景**:两个后端仓(api/db)已接入。前端仓要接时面对一个选择——是复制 api 的 Jenkinsfile,还是重新设计。

**决定**:复制形状,按"这个仓**有没有 schema 所有权**"决定增减:

| 差异 | api 版 | web 版 | 依据 |
|---|---|---|---|
| `Fetch Schema` | 有 | **删** | 前端不读库,db 仓的 schema 契约与它无关 |
| `Migrate` | 有 | **删** | 没有表结构要迁移。**"迁移该不该进 CI"不是流水线规矩,是这个仓有没有 schema 所有权的函数** |
| `Test` | pytest(82 用例,打真 PG) | **换成 `Typecheck`**(`tsc --noEmit`) | 前端当前 0 个测试;类型检查是现阶段最有价值且最快的失败信号 |
| 镜像形态 | 单阶段 python+uvicorn | **多阶段**:node:20-alpine 构建 → nginx:1.27-alpine 只装 `dist/` | 前端产物是静态文件,运行容器不需要 node、不需要源码、不需要 devDependencies |
| buildah PVC | `buildah-storage-platform` | **新建 `buildah-storage-web`** | vfs 驱动下两个 job 共用一块 PVC 且并发时,会同时改写 `/var/lib/containers/storage`。`disableConcurrentBuilds()` **只管单个 job 内部** |
| post 告警钩子 | 有(`notify_alertmanager.py`) | **本轮不做** | web 的构建容器是 node,没有 python;照搬得再加一个容器,或把 notify 改写成 node |

**代价**:两条流水线的共性(Test→Build→写回 GitOps 的顺序、首次落地守卫)被复制了两份,
将来改一处要改两处。**这是刻意的**:现阶段"看得懂"的收益大于"不重复"的收益。
真要收敛,正确方向是把 Jenkinsfile 抽成共享库(`shared-library`),那是另一件事。

**关联**:踩坑 P37/P38/P39;教材 `platform/docs/02_接入层.md`、web 仓的 `ci/README.md`。

---

## D28:前端**不发 `X-API-Key`** —— 已知缺口,**本轮不修**

**背景**:web 部署起来后,集群内 5 条断言里只有一条是红的:

```
GET /api/v1/backends -> 401 {"code":"unauthorized","message":"缺少 X-API-Key 请求头"}
```

根因是 `ai-infra-platform-web/src/api/client.ts` 的 `request()` **只设 `Content-Type`,从不发 `X-API-Key`**。
而生产环境是 `APP_ENV=production` + `AUTH_REQUIRED=true`(见 `src/main.py` 的 fail-fast)。

**决定**:**本轮不修**。理由:

1. 本轮目标是"把 web 接入流水线并验证部署",不是"做完前端鉴权"。改了会同时动
   前端源码 + 重新构建 + 重新发布,把两件事混成一件。
2. 这个缺口**本身有教学价值**:它把"接入层的认证"与"控制层的认证"这两道门**同时暴露出来**——
   它们都读 `X-API-Key`,但密钥来源不同(配置 vs 数据库)、语义不同(放进来 vs 能调哪个端点)。
3. 修它需要先想清楚一个问题:**控制台的 key 从哪来?** 让操作员手输?
   存 localStorage?这本身是一个设计决策,不该顺手做掉。

**代价**:**当前 web 控制台只能加载页面、能看 `/healthz`/`/readyz`,看不到任何真实数据。**
`Overview` / `Backends` / `Requests` / `Routing` 四个页面全是 401。
**必须如实这么说,不能讲成"控制台打通了"。**

**关联**:教材 `platform/docs/02_接入层.md` §八;`OPEN-DECISIONS` 应记一条。

---

## D29:回滚演练的**取证口径**:用构建期注入的 `APP_VERSION` 作容器内证据

**背景**:本项目此前只验证过"往前发布"(见 D26 与教材 09),**回滚从未实测**
(`OPEN-DECISIONS` 的已知缺口之一)。要补这一段,先要解决"怎么证明现在跑的是哪一版"。

**决定**:沿用 sre-lab 在 M4 里验证过的口径,但换成 platform 的载体:

| 层 | 证据 | 分量 |
|---|---|---|
| ① 声明侧 | Git 里的 `image:` 行 + ArgoCD `sync.revision` | 只能证明"声明了什么" |
| ② 工作负载侧 | Pod 的 `image` | 只能证明"调度器让它跑哪个 tag" |
| ③ **容器内侧** | `printenv APP_VERSION` | **证明"跑起来的镜像内容确实是那一版"** |

第 ③ 层的载体是 Dockerfile 里的 `ARG GIT_SHA` + `ENV APP_VERSION=${GIT_SHA}`,
由 Jenkins 用 `--build-arg GIT_SHA=${SHORT_SHA}` 注入——**版本在构建期焊进镜像内容**,
运行时改 env 改不动它。这与 sre-lab exporter 的 `sre_lab_build_info{revision=...}` 是同一个原理。

**取证手段**:本机没有 `kubectl`,用 `kubernetes` python 客户端的
`stream()` + `connect_get_namespaced_pod_exec` 读容器内环境变量(`_scripts/exec_pod.py`)。

**实测结果(2026-09-17,双向各一次)**:

| 方向 | 声明 | 容器内 `APP_VERSION` | 功能面 |
|---|---|---|---|
| 去程 | `a737d41 → 913e57a` | `913e57a` | `/readyz` 200,`schema_version=1` |
| 回程 | `913e57a → a737d41` | `a737d41` | `/readyz` 200,`schema_version=1` |

**代价与边界**:
- api 有 `APP_VERSION` 可用,**web 没有** —— 它没有自报版本的端点。
  用 web 做演练主体时,可区分证据只剩 ①②两层。**这条限制要如实说,不能假装第三层也有。**
- 演练是**手工**发起 GitOps 提交,不是 `git revert` 某次 CI bump(本仓历史里
  CI bump 与人工 fix 交错,一次干净的 revert 已不可得)。**机制等价,但要说清做了什么。**

**关联**:踩坑 P39;教材 `platform/docs/07_执行层.md` §7、
`sre-lab-local/01_排障记录/M4_发布回滚演练.md`(同一口径在 sre-lab 侧的原始实现)。

---

## D30:控制台凭据存 **`sessionStorage`**;并把"密钥展示"与"密钥凭据"分成两件事

**背景**:web 部署起来后,集群内断言里唯一红的是
`GET /api/v1/backends → 401 缺少 X-API-Key 请求头`——控制台四个页面全 401。
根因是 `src/api/client.ts` 的 `request()` 从不发 `X-API-Key`,而生产是 `AUTH_REQUIRED=true`(见 D28)。

**决定**:

1. **前端新增一个凭据存储**(`src/api/apiKey.ts`),存 **`sessionStorage`**,
   不是 `localStorage`。理由:`localStorage` 是"**长期凭据**"语义——把一把 admin key
   永久留在机器上;`sessionStorage` 随标签页关闭消失,是"**本次会话的凭据**"语义。
   两者都挡不住 XSS,**这一点不粉饰**:正确解法是 `httpOnly` cookie + `SameSite` + CSRF 双保险,
   本项目刻意不做。选 `sessionStorage` 是**中间点,不是安全终局**。
2. **`request()` 挂 `X-API-Key`**——只在**一个** choke point 改,不在各个 endpoint 里散落。
3. **密钥一变,全站重取**:把 apiKey 纳进 `useAsync` 的依赖数组,于是所有页面的 `useAsync`
   自动重取。**一个 hook 改一行,四个页面的行为就对了。**
4. **不做"nginx 直接把 key 注入请求头"**——那样浏览器完全碰不到密钥,但**所有人都变成 admin**,
   而且控制台演示不了"错 key 被拒""scope 不同"。本项目是**教学标本,鉴权过程必须看得见**。

**代价**:密钥对浏览器里任意脚本(XSS)可读;`useAsync` 隐式依赖全局密钥状态
(源码零注释,只能靠教材说);密钥一变在跑的请求会被 abort 重发。

**不变量(验收口径)**:✅ 正确 key → 200 且读到真实数据;✅ **错误 key → 仍 401**。
**只测"能通"不够**——没有反向对照,就无法区分"鉴权生效"与"鉴权被绕过"。

**关联**:教材 `platform/docs/01_入口层.md` 决策 11、`platform/docs/02_接入层.md` 失败模式 6;
踩坑 P40/P41。取代 D28 里"本轮不修"的处置。

---

## D31:CI 构建容器与镜像构建阶段统一 **node 22**,并把下限写进 `engines`

**背景**:见 P40——本地 node 24 解析出的 `jsdom@30`(要求 node ≥22.22.2)在 CI 的 `node:20-slim` 上运行时炸。
同时 `node:20` 已 EOL(2026-04),而本仓 `@types/node` 本来就是 `^22`,**类型与运行时一直不一致**。

**决定**:

1. `Dockerfile` 构建阶段与 `ci/Jenkinsfile` 的 `node` 容器:**统一 node 22**。
2. **降依赖还是升运行时?——两个方向都用实测决定**;先起一次性 Pod 量出真实版本:

   | 镜像 | 实测 `node -v` | 结论 |
   |---|---|---|
   | `node:22-slim` | **v22.23.2** | ≥ 22.22.2 ✓ 且镜像能拉到 |
   | `node:22-alpine` | **v22.23.2** | 同上 |

   22.23.2 满足 `jsdom@30` 的 `^22.22.2` → **依赖保持最新,不降级**。
3. `package.json` 加 `"engines": { "node": ">=22.22.2" }`,把下限变成**机器可读**。

**代价**:镜像基线变了,构建要重新拉 `node:22-*`(首次会慢);node 22 在 2027-04 也会进 EOL,
到时要再升一次。**这是必然的成本,不是这次做错了什么。**

**为什么不用 node 24**(与本地完全一致):本仓 `@types/node` 声明的是 `^22`,
把它改成 24 是**另一个决定**(要连带升类型包)。选 22 是"跟着仓库已有的声明走",
改动面最小且自洽。

**关联**:踩坑 P40;教材 `platform/docs/01_入口层.md` 决策 12。

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-16 | 建立本文件,回填 D1–D5 |
| 2026-09-16 | M0 诊断后新增 D6(M1 处理存量方式)/ D7(内存约束)/ D8(token 泄露) |
| 2026-09-16 | M1 审计后新增 D9(ollama 冲突)/ D10(Namespace 接管) |
| 2026-09-16 | M2 降噪后新增 D11(不吊销 token,取代 D8 吊销部分)/ D12(适配器选型)/ D13(降噪方式)/ D14(监控栈管理边界) |
| 2026-09-16 | M2 通道打通后新增 D15(邮件接收端,取代 D12 适配器方案);实测投递成功 |
| 2026-09-16 | M3 起步后新增 D16(镜像仓库选型:本地 k3d registry)/ D17(GitOps 写回用仓库级 deploy key) |
| 2026-09-17 | 控制面加固后新增 D18(Git 留集群外 + Service/Endpoints 声明式解析)/ D19(不拆双仓)/ D20(单测用标准库 unittest)/ D21(Jenkins 插件缓存推迟)。对应踩坑 P24–P27 |
| 2026-09-17 | 端到端验证收尾后新增 D22(WSL 副本是编辑源、不强行对齐远端)。对应踩坑 P28–P31 |
| 2026-09-17 | **platform 三仓接入流水线**后新增 D23(数据层搬进集群,取代投影外部 PG)/ D24(首把 admin key 带外引导)/ D25(schema 迁移纳入 CI 且排在构建前)/ D26(镜像 tag 归 CI 管)。对应踩坑 P32–P36;新增教材 `platform/docs/09_交付层.md` |
| 2026-09-17 深夜 | **web 仓接入流水线 + 首次回滚演练 + 补齐五层教材**后新增 D27(前端接入的"砍两节加一节")/ D28(前端不发 `X-API-Key`,本轮不修,记为已知缺口)/ D29(回滚演练取证口径:构建期注入 `APP_VERSION` 作容器内证据)。对应踩坑 **P37–P39**;新增教材 `platform/docs/02_接入层.md` / `05_调度层.md` / `06_编排层.md` / `07_执行层.md` / `08_观测层.md`,**九层教材至此补全** |
| 2026-09-17 深夜(续) | **前端补鉴权 + 补单测**后新增 D30(控制台凭据存 `sessionStorage`;区分"密钥展示"与"密钥凭据";反向对照"错 key 仍 401")/ D31(CI 与镜像构建统一 **node 22**,并把 `engines` 下限写成机器可读)。对应踩坑 **P40**(本地/CI Node 版本不一致)/ **P41**(`npm install` 挂住且只改了一半)。前端单测 **0 → 20 条**,并入 CI 的 `Verify` 阶段 |
