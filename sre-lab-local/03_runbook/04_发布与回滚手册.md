# 发布与回滚运行手册

> 回答四个问题:**谁触发**、**制品怎么来的**、**怎么证明部署成功**、**出事怎么退回去**。
>
> 完整演练过程与证据见 `01_排障记录/M4_发布回滚演练.md`。

---

## 一、先分清两条路

| | 命令式(不要用) | 声明式(本手册) |
|---|---|---|
| 怎么发布 | `kubectl set image deploy/... image=xxx` | 改 Git 里的清单,让 ArgoCD 收敛 |
| 怎么回滚 | `kubectl rollout undo` | `git revert` 那次 bump 提交 |
| 谁能知道 | 只有动手的人 | Git 里有记录 |
| 会不会被拉回 | — | **会被 ArgoCD 秒级拉回** |

> **GitOps 下的铁律:手工 `kubectl` 改动是"无效动作"。** 实测:手工 `set image` 改成别的 tag,**t+5s 就被拉回**。ArgoCD 对**它管理的资源**有 watch,3 分钟只是兜底的全量对账。
>
> 所以在排障时看到"我明明改了啊",先问一句:**这个资源归 ArgoCD 管吗?**

---

## 二、一次标准发布

### 触发
推送到 **Gitea**(`http://127.0.0.1:3001/fei232401/sre-lab.git`),webhook 会通知 Jenkins。

> ⚠️ **不是 GitHub。** `origin`(GitHub)只是对外镜像,CI 环路走的是 Gitea。

### 流水线做了什么(`ci/Jenkinsfile`)

```
Checkout → Trigger Guard → Resolve Tag → Lint → Build → Push → Verify In Registry → Update GitOps
```

| 阶段 | 要点 |
|---|---|
| Trigger Guard | 本次提交**没触及** `ci/` 或 `03-ollama-exporter/` 就直接早退(见第四节) |
| Resolve Tag | 镜像 tag = **源码仓 7 位提交哈希**(`git rev-parse --short=7 HEAD`) |
| Build | `buildah bud`,并注入 `--build-arg GIT_SHA=${SHORT_SHA}` |
| Verify In Registry | 回读 registry 的 tags 列表,确认镜像**真的登记上了** |
| Update GitOps | `sed` 改清单里的 image → commit → push(带 3 次重试 + rebase 重放) |

### 观测点(发布后逐条确认)

```bash
# 1) 声明侧:Gitea main 是否前进
curl -s http://127.0.0.1:3001/api/v1/repos/fei232401/sre-lab/branches/main

# 2) 收敛侧:ArgoCD 认到的 revision
kubectl --context k3d-ai-cluster -n argocd get application monitoring-app \
  -o jsonpath='{.status.sync.revision}{"\n"}'

# 3) 运行侧:Pod 实际用的镜像
kubectl --context k3d-ai-cluster -n ai-platform get pods --no-headers | grep ollama-exporter
kubectl --context k3d-ai-cluster -n ai-platform get pods \
  -o jsonpath='{range .items[*]}{.metadata.name}{" "}{.spec.containers[0].image}{"\n"}{end}' | grep ollama-exporter

# 4) 行为侧:让程序自己报版本(见第三节)
```

**收敛耗时**:约 1–2 分钟,取决于 ArgoCD 3 分钟轮询的相位。

---

## 三、怎么证明"跑的是新版本"

exporter 暴露:

```
sre_lab_build_info{revision="a14ce79"} 1
```

版本号是**构建期**注入的(`--build-arg GIT_SHA`),因此**镜像内容本身携带版本**,换个 env 并不会"变成"另一个版本。

取值方式(**容器里没有 `wget`/`curl`,用 python**):

```bash
POD=$(kubectl --context k3d-ai-cluster -n ai-platform get pods --no-headers | grep ollama-exporter | awk '{print $1}')
kubectl --context k3d-ai-cluster -n ai-platform exec $POD -- python -c "
import urllib.request
t = urllib.request.urlopen('http://127.0.0.1:9101/metrics', timeout=10).read().decode()
print([l for l in t.splitlines() if 'build_info' in l])"
```

**为什么要这一步**:Pod 上的 tag 只能证明"调度器让它跑这个 tag",不能证明**它真的起来了**、更不能证明**镜像内容对**。指标是从容器内部经真实数据面取回来的。

---

## 四、回滚

### 正确做法:改声明

```bash
cd ~/projects/sre-lab
git fetch gitea main && git merge --ff-only gitea/main      # 先对齐 CI 写回的提交
git revert --no-edit <bump-commit>                          # 例:ci: bump ollama-exporter to a14ce79
git push <gitea-url> HEAD:main
```

然后**等 ArgoCD 收敛**(1–2 分钟),再按第三节采行为证据。

- ✅ 用 `git revert`,**不要** force push:历史保留,谁在什么时候退的、为什么退的,可追溯
- ✅ 回滚后证据应表现为"**新指标消失**"(旧镜像没有这个能力)——这是结构性差异,比"值变回去了"更硬
- ❌ 不要 `kubectl rollout undo`、不要 `set image`:会被 ArgoCD 拉回,而且**你会在日志里看到自己白忙一场**

### 应急:真的要绕过 ArgoCD 时

只能临时关掉该 Application 的自动同步(`kubectl -n argocd patch app ...`),**并且必须知道这是一次例外**,事后要么把改动补进 Git、要么把 auto-sync 打开。手册不推荐这条路,列出来只是为了在真出事时别乱。

---

## 五、触发方式:webhook(唯一触发源)

**现状:唯一的触发源是 webhook。SCM 轮询已从任务配置里彻底移除。**

```
Gitea push → webhook → POST /generic-webhook-trigger/invoke?token=... → Jenkins
```

| 项 | 值 |
|---|---|
| 端点 | `/generic-webhook-trigger/invoke`(由 `generic-webhook-trigger` 插件提供) |
| 令牌 | Jenkins 凭据 `gitea-webhook-token`(值不入 Git) |
| Jenkins 入口 | NodePort `30080`,地址 `http://k3d-ai-cluster-server-0:30080`(Gitea 按节点容器名访问) |

### ⚠️ 三个前置条件,少一个就静默不工作

| # | 条件 | 漏了会怎样 |
|---|---|---|
| 1 | `generic-webhook-trigger` 插件已装 | 端点 404 |
| 2 | Jenkins 里有 `gitea-webhook-token` 凭据 | 端点 404(错 token 也 404) |
| 3 | **Gitea 的 `ALLOWED_HOST_LIST` 放行本网段** | **投递被拒,且只在 Gitea 日志里报错** |

第 3 条是 SSRF 防护,详见 `06_踩坑记录.md` 的 **P23**。Gitea 的 `app.ini`:

```ini
[webhook]
ALLOWED_HOST_LIST = 172.18.0.0/16
```

> **为什么不用轮询**:原始动机是轮询在这套"临时 agent"架构下会静默失效(详见 P18)。**后来发现那句结论说大了**——轮询其实是"缓存 + 需重启"的问题,不是不能用。但即便修好,**推送模型能给出轮询给不了的东西**:webhook 载荷里有 `before` 和 `after` 两个 revision,所以 Trigger Guard 能算"**整次推送**改了哪些文件",而不是只看最后一个提交。**这才是采用它更硬的理由。**

### 怎么验证链路(不要只测端点)

**手动打端点返回 200 `triggered:true`,不能证明链路通**——那绕过的是 Gitea 自己的策略(P23 就是这么骗过我的)。

**唯一可信的验证是推一次真代码,然后看构建的"触发原因"**:

```bash
curl -s -u "$JU:$UP" "$JH/job/sre-lab-ci/17/api/json?tree=actions[causes[shortDescription]]"
# 期望看到: Gitea push refs/heads/main (<sha>) by <user>
# 只看到 "Started by user ..." = 有人手动点的,链路没验到
```

### ⚠️ 已知偏差:这些配置不在 Git 里

| 什么 | 在哪 | 重建集群后 |
|---|---|---|
| Gitea 的 webhook 定义 | Gitea 数据库 | **会丢**,需按本节末命令重建 |
| Jenkins 的 `gitea-webhook-token` 凭据 | Jenkins PVC | 若 PVC 一起丢,需重建 |
| Gitea 的 `app.ini` | Docker volume `gitea-data` | 同左 |

**如实记录,不假装这是 IaC。** 这三样都是"**集群外的一次性手工配置**",它们的丢失**不会报错**,只会让链路安静地不动——**这正是 P18 的教训在另一个地方重演。**

### 自激循环的防护

webhook 不认路径,所以 CI 自己写回配置仓的那次提交**也会触发一次**流水线;由 **Trigger Guard** 认出"本次只改了 `sre-lab-gitops/`"并早退——循环**只走一次就停**,而不是无限。见第六节。

> 实测(#16 → #17):push `81cc34e` 触发 #16 构建并写回 → 写回那次 push 触发 #17 → **#17 判定跳过,循环终止**。

---

## 六、Trigger Guard(为什么需要它)

```groovy
base    = webhook 给的 GIT_BEFORE(取不到就退回 HEAD~1)
changed = git diff --name-only <base> HEAD
relevant = changed 里是否有 ci/ 或 03-ollama-exporter/ 开头的文件
```

- **命中** → 继续构建
- **不命中** → `env.SHOULD_BUILD=false`,后续 stage 全部 `when { expression { env.SHOULD_BUILD == 'true' } }` 跳过
- **无法比对**(首个提交 / force push 后 `before` 不在本地)→ **照常构建**(fail-open)

### 为什么 diff 的起点要用 webhook 给的 `before`

原实现是 `HEAD~1..HEAD`,**只看最后一次提交**。推 3 个提交、最后一个只改了文档、前两个改了源码时——**它会被漏掉**。

webhook 的载荷里有 `before`(推送前的 revision)和 `after`(推送后的),**用它算 diff 才覆盖整次推送**。

> **这正好回答"为什么要换 webhook"**:轮询只能告诉我们"**变了**",它给不出两个 revision。**`before`/`after` 是推送模型结构上才有、轮询模型给不了的东西。**

实测(构建 #17 控制台):

```
本次推送范围: 81cc34e..bf43ff9(由 webhook 提供)
本次推送涉及的文件: sre-lab-gitops/production/monitoring/ollama-exporter.yaml
本次推送未触及 ci/ 或 03-ollama-exporter/,跳过构建
```

> **为什么 fail-open**:漏构建比多构建风险大;而且我们刚刚才被"静默跳过"坑过一次。
>
> **边界**:手动触发构建时没有 webhook 变量,退回 `HEAD~1`——**只看最后一次提交**。这条简化在手动触发这一条路径上还在。

### ⚠️ 一个差点漏掉的坑:`git` 默认会**转义非 ASCII 路径**

`git diff --name-only` 遇到中文文件名时,默认输出的是**加了引号的八进制转义形式**:

```
"sre-lab-local/01_\346\216\222\351\232\234\350\256\260\345\275\225/..."
```

而闸门是靠 `p.startsWith('ci/')` 判定的——**首字符变成了 `"`,匹配必然失败**。

> **后果:文件被静默漏掉,流水线不报错、只是不构建。又是 P18 那个形状。**

当前 `ci/` 和 `03-ollama-exporter/` 下都是 ASCII 文件名,所以**没有发作**;但只要有人在构建路径下放一个中文名的文件,就会踩中。

**修法**:所有 `git` 调用带上 `-c core.quotePath=false`。

```groovy
def GIT = 'git -c core.quotePath=false'
```

> **这类"没发作的 bug"最值得记。** 它不会在测试里暴露,只会等到某个特定的人做了某件特定的事,然后**安静地什么都不发生**。发现它的方式是**去看真实输出长什么样**,而不是去读代码逻辑。

---

## 七、故障速查

| 症状 | 先查什么 |
|---|---|
| 推了代码但没构建 | **先看 Gitea 容器日志**(`docker logs gitea`)——投递失败只在这里报;再确认 webhook 还在、三个前置条件都满足(第五节) |
| 手动打端点通、真实推送不通 | **Gitea 的 `ALLOWED_HOST_LIST`**(P23)。手动测绕过的是 Gitea 的策略,不是网络 |
| 构建成功但集群没变 | ArgoCD 的 `sync.revision` 前进没有;清单里的 image 改没改 |
| 手工改了镜像又被还原 | 正常——ArgoCD selfHeal,见第一节 |
| 部署了但"看起来没变" | 用第三节的指标证明,别只看 tag |
| 循环构建 | Trigger Guard 是否失效;CI 写回是否被误判成构建路径 |
| Jenkins 起不来 | init 容器在重下插件,**分钟级**,先等(见 `03_Jenkins_运行手册.md` 第四节) |

---

## 变更记录

- 2026-09-16:建立。触发方式由 SCM 轮询改为 webhook(**轮询已从任务配置中移除**);新增 Trigger Guard;新增构建期版本注入(`sre_lab_build_info`)
- 2026-09-16 晚:补上 Gitea `ALLOWED_HOST_LIST` 前置条件(P23);Trigger Guard 改用 webhook 的 `before`/`after` 判定整次推送范围,取代原来的 `HEAD~1` 简化;端到端实测通过(#16 构建 / #17 跳过)
